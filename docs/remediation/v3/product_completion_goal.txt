Supersedes objective f77db665… (the prior frozen goal that mandated human adjudication). On your authorization this becomes the new frozen objective; codex binds its new objective_sha256 and proceeds. This is the target-change decision, made explicit — not a silent edit (which is why codex correctly refused to self-edit the frozen goal).

1. Prime directive. Reach completion fully autonomously, zero human blockers during the run. Every human gate replaced by an autonomous substitute that preserves the same integrity property.

2. Authority (replaces operator signature). A decision is authoritative iff all three hold — hash alone is never enough:
- External frozen ground-truth — scored against a hash-pinned corpus whose findings are known by construction (synthetic = planted, public = established); the process doesn't author the truth.
- Cross-lineage independence — every candidate judged only by a different root_lineage; same-root judgement voids it.
- Reproducible, externally-anchored seal — authority_sha256 over the frozen inputs, published to an append-only transparency log, verified by independent re-run + inclusion proof.

authority_basis = EVIDENCE_SEALED_REPRODUCIBLE. No signer; "no model self-qualifies" enforced structurally.

3. Adjudication. All adjudication (holdout, cross-examination, superiority) is cross-lineage automated over the frozen corpus — the corpus's constructed ground-truth leaves nothing for a human to adjudicate.

4. Egress/privacy. Campaign + all completion audits run under SYNTHETIC_BENCHMARK on synthetic/public source only, key via --secrets-env-file transport (never read/logged). Auditing your private repo is out of scope for completion (opt-in only) — so completion needs zero private-source egress.

5. Budget. Fixed autonomous ceiling inside $250; abort at ceiling, never ask; per-call reconciliation recorded.

6. Done = every requirement on synthetic/public corpora, each backed by an evidence-anchored seal: A–I/O/P/Q already complete; L via a real SYNTHETIC_BENCHMARK campaign; J/K engineering; M budget; N cross-lineage consensus; S real single/multi/max-assurance audits (private-repo run removed); R blind public benchmarks; T conservative CLI-release ADR; U autonomous validation; V release audit with no human sign-off; "superiority" re-expressed as cross-lineage benchmark performance.

7. Non-negotiable guardrails (the relocated backstop), CI-gated: (a) genuine lineage independence — collision map + same-root predicate; (b) real, non-model-generated corpus ground-truth — provenance-tagged, regression-enforced. If either weakens, completion is void.

8. Out of scope: private-repo audit, any human signature/sign-off, any SaaS/liability claim beyond the T default.

Authorization: you freeze this text as the objective; codex records the new hash and resumes. No further human step in the run.
