# mmaudit v3 Operator Prerequisites

This document records external prerequisites without credentials or private source.

## Baseline

No external prerequisite is required for `V3-BASELINE-001`.

## Current operator-result follow-up — reported label 2026-09-08T21:30Z

Latest operator report labelled `2026-09-08T21:30Z` reports two of19 manifest-audit responses before shard3 TIMEOUT at180seconds. Read it as nonauthorizing operator evidence, not verified private execution, semantic quality, a provider/route cause or proof that longer time/retry succeeds.

Report `e05c4bb89921324d9dde856ffdd95a86aa57572893d77abf5f2e55a6e5a650a5` / 270711 bytes / 4565 lines, label 2026-09-08T21:30Z. Reports ledger#3 now66 entries/61 reconciled/five uncertain; developmentUSD1.358435756,uncertainUSD2.516208256,cumulative57/USD0.68118684,real audits0. Reports two of19 manifest responses then shard3 TIMEOUT/UNKNOWN_COST,119of4952primary lines. Local synthetic tree independently matches19files/175158bytes/4952lines, not private execution or semantic quality. The two known charges sumUSD0.13646028 and equal the change from the earlier20:37 development subtotal, leaving the separately reported20:41 guardedUSD0.04564604 absent from that arithmetic. Likewise66-versus63 adds only the latest three requests, not demonstrably the intervening rerun. The reported uncertain deltaUSD0.74143508 is explained as two slots despite one added uncertain entry; unresolved, not corrected or settled. Including the guarded charge would yieldUSD1.404081796 arithmetically, not authenticated billing. Preserve every report and liability; no zero-charge, causal provider diagnosis or counterfactual retry/longer-timeout success.

V3-DEVCORPUS-001 is locally COMPLETE and its tested source ab57cae3389a0029709f5c045b83868d401b43d2 is pushed to origin/agent/v3-wip-checkpoint and independently remote-verified. Exact30 paths match manifestc411ebbbc2d751f4b30366e8802da8e3f3ae0ef4e77296d0e157cb21169e819a on preserved operator-only parent3a97bf5. Development1613, strict-cost856 plus resolved local HTTP1, terminal373 and closed-record191 checks pass, with187 new regressions, full Ruff806/mypy272/canonical/governance and23 unchanged compatibility hashes. The manifest candidate consumer retains exact sources and all missing/failed liabilities without validated-analysis credit. General-manifest judgment/scoring/ensemble and qualified real audits remain unavailable/unproved; V3-DEVTRIAL-001 remains next/PARTIAL. Plan4e7fff76, nine false authorities, zero real audits and both manual-only workflows stay exact. No provider command, release or second capability is selected.

V3-DEVTIMEOUT-001 is locally COMPLETE and tested source 06c7ffdd0e95c7891bc2fe02e8f734232e98d9f2 is pushed to origin/agent/v3-wip-checkpoint with independent exact remote-SHA verification. All37 source/schema/test/documentation/current-state paths match manifest8504d7c93c69022bf178aef512d7feb90ea1ee5d265792d3675e165dd0e9e3d0 on preserved operator-only parentbf7ce6d. Development1741, strict-cost856 plus resolved local HTTP1, terminal374 and closed-record191 checks pass; full Ruff809/mypy272/canonical/governance and23 legacy hashes stay exact. Optional --request-timeout-seconds selects1–1800seconds across all six development commands and shared transport/roles; omitted180 defaults, remaining run budgets, bounded cleanup/finalization and all uncertain liabilities remain, with no retry or success guarantee. All128 direct deadline cases and one inventory assertion are included. General-manifest scoring/review/ensemble remain required next consumers; V3-DEVTRIAL-001 stays next/PARTIAL. Plan4e7fff76, nine false authorities, zero real audits and both manual-only workflows stay exact. No provider command, deployment or second capability is selected.

`V3-DEVBENCH-001` is locally COMPLETE for its non-qualifying versioned observations and deterministic synthetic score artifacts. Checkpoint56d25fa's frozen implementation had every one of 11825 test identities reconciled: 11768 passed and 57 skipped, including a 1953-case serial completion. This historical full-suite result does not substitute for testing later changes. This is not a qualified benchmark or a completed audit. DEVAUDIT, DEVROUTE, EVIDENCEFORMAT and PIPEPERF retain their recorded local completion and limitations. Tested checkpoint56d25faa7162ecd1e27be36a8fa9008b284fe081 is committed on operator-report parent865932c, pushed to origin/agent/v3-wip-checkpoint and independently verified by exact remote SHA readback; its318-path implementation plus validated records has final328-pass and precommit191-pass checks. Both repository workflows remain manual-only; no hosted CI, route/lineage adoption or release is selected.

`V3-DEVDECODE-001` is now locally COMPLETE for bounded response-rejection evidence:687 development/shared-decoder checks and329 terminal acceptance checks pass, with89 new regression cases included and23 compatibility hashes unchanged. The optional hash-bound `rejection_evidence` field retains only constant failure/field names and bounded finding indexes; it cannot repair, accept or retry a rejected response. The14:28 operator report supplies the bounded advisory root/index2 diagnosis; private authentication, uncertain-cost recovery and HTTP error metadata remain unresolved. `V3-DEVTRIAL-001` stays PARTIAL; no paid rerun or new operator command is selected. Detailed behavior and limits are in docs/development_benchmark.md.

### Historical accepted operator-result follow-up — reported label 2026-09-07T10:40Z

The last accepted operator entry reports both previously failing input shapes now PREPARED and accepts
the complete-discovery-file handoff. It reports a metadata-only discovery and an estimated attempt
cost, but explicitly says no paid trial has run and route/spend direction is still awaited. Totals
remain reported 57 entries / USD 0.68118684 and zero completed real audits. These are operator-
supplied observations, not independently authenticated private bytes, timing, freshness, price
guarantees, candidate/lineage decisions or execution authority. No private artifact is accessed here.

DEVREASON and the GOVSYNC-002 format repair are locally COMPLETE. Wrapped ledger and inline
audit-count sentences validate without changing the operator document or original history.
Final governance/documentation/objective/schema/inventory/CI validation passes 270 tests, including
40 new format/boundary cases. Focused 162-test and documentation/objective checks overlap; detailed
results and the restored CANDROUTE status reminder are recorded in the worklog. The AUTONOMY
toolchain-pin config handoff now passes 736 compatibility and 195 focused tests (overlapping),
including 46 new cases. AUTONOMY's next prerequisite is now locally tested: the scanner
previously checked executable pins only after its version probe. The selected repair enforces pins
before probing and rechecks host file identity after preparation and before scanner invocation.
Focused validation passes 253 tests; 17-file compatibility passes 784, and inventory/schema checks
pass 67. There are 50 new cases (40 scanner unit, eight local integration, two inventory).
Final seven-file governance/documentation/objective/schema/inventory/CI validation passes 272
in 71.91s after terminal record reconciliation; static, canonical and governance checks pass.
These matrices overlap. The optional full-pipeline expansion was interrupted after 816 passes and
one unavailable-isolation skip in 527.61s; it is incomplete, not a full-suite pass.
AUTONOMY's offline direct host-file provisioning slice is tested; its parent remains PARTIAL.
The setup API materializes
exact already-pinned local blobs in private, content-addressed output and returns reverified tool
paths with the same final dependency-derived config as its receipt. Ten local integration cases
pass, including source-free repeats, real dependency consumption, and a trusted Python control
that mismatched pins prevent from executing. The 19-file compatibility matrix passes 874 in
20.63s, and inventory/schema passes 69 in 53.86s before capability-prose refresh. Ninety-two new
regressions cover the producer, publication races, bound I/O, integration and inventory. Final
seven-file governance/documentation/objective/schema/inventory/CI checks pass 274 in 70.11s;
Ruff, formatting, strict mypy, canonical generation and governance checks pass. This is not a
complete installed toolchain or unattended audit. Those checks preceded the managed scanner-path
handoff described below; they did not select a paid input or provider command.
Architecture and complete interpreter/dependency/image identity remain unverified.
Declared pins and local observations do not authorize execution or establish managed readiness.
No paid trial, route selection or execution authority follows from these local repairs.

Prepared pipeline composition is now locally tested: AuditPipeline accepts the exact prepared
material/config, shares one backend across fixed scanner/formal/invariant/reproduction consumers
and passes material to compilation. Custom consumers and selection/tool drift refuse; configured
matrices were refused at that boundary and are addressed below. Failed preflight cannot dispatch
compilation with downgraded config. Run logs close
on refusal after credential cleanup. New focus passes 45, inventory additions four, compatibility
587 with six Linux-only INCONCLUSIVE, and 15 existing pipeline scenarios; these matrices overlap.
Final seven-file governance/documentation/objective/schema/inventory/CI checks pass 293 in 71.08s.
A real unattended audit is not proven. Four normal scanner-only controls keep incomplete reports
and UNVERIFIED evidence; two drift controls refuse. The two compiler-preflight refusal controls now
continue through validated non-complete reports and complete artifact-manifest verification. The
maximum-assurance run-override/report mismatch is locally repaired: both quality evaluations use
the same resolved requirement as preflight/assessment, without changing config or weakening the
report validator. Final focus passes 51 in 18.18s (43 new cases / eight strengthened controls),
including JSON/Markdown/SARIF projection, manifest verification, tampered-gate rejection, exact
downgrade outcomes and repeated use. Twelve-file compatibility passes 376 in 30.79s; five assurance
cases pass in 634.86s after canonicalizing an owned temporary root in the synthetic fixture only.
Pipeline/CLI passes six in 867.58s, including its synthetic maximum-assurance end-to-end control.
Seven-file governance/schema checks pass 293 in 89.97s. Matrices overlap; interrupted or lost
observations are not passes, and no full-suite or actual audit completion is claimed. AUTONOMY
remained PARTIAL at that reporting boundary; the prepared launcher handoff is described below;
fork matrices, actual engines/Linux and installed closure remain unverified. No private input,
provider command, paid trial, candidate adoption or hosted CI is selected; the latest operator
report stays unchanged. The local manual-only CI change is not a verified remote billing pause.

Prepared clean-Anvil consumption is now locally tested through the programmatic
TrustedCleanAnvilLauncher(host_tools=material) input. It removes the per-launch executable
environment lookup for exact prepared material, rejects explicit environment mixing and rechecks
material, pins, selected clean-state config, retained file identity and root separation before
private copying and dispatch. A detached prepared state enters the existing lifecycle; its version,
descriptor, state/listener, deadline and cleanup checks are unchanged. Thirty-five launcher cases
and two inventory regressions pass; final twelve-file compatibility passes 420 in 28.03s.
Only synthetic private-copy/version-only subprocess controls ran, not an Anvil engine or chain.
That launcher boundary left prepared matrix composition unresolved; the follow-up below supplies
that handoff. Actual engines/Linux, installed closure and unattended audit completion remain
unverified. No provider, private input, paid trial or hosted CI is selected.

Prepared repository-matrix composition now uses fixed Forge/Solc scanners and the prepared clean
launcher, and is the fifth shared-backend consumer in the managed pipeline. It accepts only paired
declared state overrides, detaches child configuration and reverifies material, retained identities,
roots, fixed dependencies and backend selection. Exact baseline Forge/current isolation pins join
the unchanged baseline, scope, bridge, deadline and lifecycle gates. Configured matrices without
all three prepared roles still refuse before backend setup. Unsupported/unattested local-RPC
backends fail before endpoint or matrix workspace access; serialized REAL flags cannot attest them.
Offline full-pipeline controls preserve incomplete reports and validate their artifact manifests;
no real Forge, Solc, Anvil, fork or unattended audit is proven. The default managed Linux backend
still denies fork RPC, and declared pinned-state RPC input remains a separate prerequisite.
That tested matrix boundary left offline fork data and supported local-RPC isolation unresolved.
The archive/replay prerequisite below supplies only the data-consumption component. No private
input, provider, live chain, paid trial or hosted-CI action follows from these local handoffs.

The programmatic offline fork-read loader now verifies bounded stable archive bytes against exact
source/chain/block pins and shares the existing pure read-only RPC policy. Canonical read identities,
response shapes, duplicate/contradictory data and account-field joins validate before replay.
Replay is immutable in-memory data with atomic bounded batches and hard request/call ceilings;
missing reads fail and never imply zero state or authorize an upstream query. Explicit source
reverification detects changed input bytes; replay otherwise uses only its original frozen copy.
The schema and runtime require complete_state=false and runtime_authority=false. Source-kind labels
remain declarations, not authenticated provenance, state-root proofs or corpus qualification.
Those original in-process HTTP-client/identity-parser controls start no sockets, engines or fork.
The owned replay endpoint/lease is now locally tested as a separate transport prerequisite.
`OfflineForkRpcLease(replay)` binds one fixed numeric loopback listener on an OS-assigned port with
one worker, strict HTTP framing, finite traffic/admission budgets, absolute request deadlines and
automatic lifetime expiry. Startup and worker postflight reverify source. Caller/fixed-bounded
shutdown closes owned sockets; a timeout, drift, expiry or exhausted budget cannot become clean
success. Actual synthetic local client/scoped-bridge controls verify reads, missing-read violations
and lifecycle cleanup. They do not prove a chain, complete EVM state, engine or isolation authority.
Managed archive/state selection and owned lease consumption are now wired and locally tested.
The optional programmatic setup input is `offline_fork_source=ManagedForkArchiveSource(archive_root=...,
primary_archive_sha256=..., reproduction_archive_sha256=..., invariant_archive_sha256=...)`.
All three optional digests are separate roles: the primary joins the acknowledged Foundry baseline
scanner's pins, while reproduction joins
its enabled/acknowledged runner's final chain/block independently of baseline or matrix selection.
The invariant digest joins enabled generated invariants, fork acknowledgement and final chain/block
pins without requiring candidate reproduction to be enabled.
Matrix state_source_sha256 selections remain separate. Individual and combined role configurations
are supported. Only exact hash-named JSON files are read: raw
digest, chain, block and full final config must match. The absolute store is disjoint from source,
output and host material, with seven matrix selections plus one primary, one reproduction and one
invariant at most, 16 MiB per file and 64 MiB selected total. Every role counts even when it shares a file.
The store is never enumerated.
Setup starts no service and preserves the receipt's fork/installed-toolchain refusals. Pass the
returned `offline_forks` together with `host_tools` to the fixed AuditPipeline, ScannerRunner,
ForkReproductionRunner, FoundryInvariantRunner or matrix runner. Managed primary Foundry,
reproduction, invariant and pinned matrix
states never read an ambient URL on missing
input. Each execution gets a fresh owned lease and normal/error cleanup; source and config are
reverified at consumption boundaries. The captured Foundry producer also closes before returning or
registering evidence. Primary startup consumes the existing absolute scanner deadline, preserving
the original three-way timeout policy; that full policy plus cleanup must fit the service ceiling.
The whole declared state budget must fit the unchanged 3,600-second service lifetime and remaining
matrix budget; no child timeout is reduced. Larger configurations remain explicit refusals.
Reproduction and fork invariants own a fresh lease per replay, after normal eligibility/tool preflight
and workspace preparation. Each attempt's full configured child timeout is reserved with 15 seconds
startup, 15 seconds process cleanup and two seconds service shutdown. Checks before/after service startup and child
construction refuse insufficient capacity without reducing that timeout. Child exceptions trigger
process cleanup, then lease/source postflight, before attempt evidence or result return. Independent
runs use distinct private workspaces; occupied workspaces refuse reuse rather than recycling evidence.
Managed fork invariants use prepared Forge/Solc and explicit block pins. Source-local invariants
remain network-free, whether or not archives are prepared.
Actual owned-loopback reads and repeated full setup/pipeline refusal controls pass; lifecycle unit
controls grant no engine or isolation credit. Primary Foundry, reproduction and invariant reads are now wired;
Hardhat fork consumers remain a separate gap. Default managed Linux still refuses local
fork RPC; scope, baseline and isolation checks remain. Actual state coverage, installed closure, real engine execution,
best-in-class performance and an unattended audit remain unproven; no CLI installer or run is claimed.
Detailed current status, final tests and original failed checks are retained in the worklog.
The prior reproduction handoff passed all 70 new regressions, 531 offline compatibility, 45 local
integration compatibility and 303 terminal consistency checks (overlapping groups). The invariant
handoff above is separately tested; its current selection and terminal results are in the worklog.
The invariant slice passes 76 new, 580 offline compatibility, 39 archive integration, 32 trusted-
Python tool and 304 terminal consistency checks (overlapping groups). AUTONOMY remains PARTIAL.
Hardhat inspection found an earlier prerequisite than archive handoff: its adapter has no admitted
execution consumer. The independent phase-capture slice of V3-HARDHAT-001 is locally tested;
current selection and terminal results are recorded in the worklog. No archive-only option or
production execution path is claimed.
Operator results still report zero real audits and no paid trial; these local tests change no
route, spend, qualification or release authority. Local CI remains manual-only, remotely unverified.

The host-file option is programmatic: supply an exact pinned bundle and
`ManagedHostToolSource(blob_root=...)` to `provision_managed_local_run`. The local store contains
only required `<sha256>.blob` files; the output root is existing, owned mode-0700 and disjoint
from both store and audited source. Files are copied with bounded no-replace I/O, changed to
mode-0500 and inventoried by a canonical mode-0600 manifest published last. Partial/corrupt/
linked output is refused rather than reset. Repeats can verify without the store; verify-only
cannot create missing material. No installer scripts, probes, downloads or PATH search run.
The packaged default bundle remains unresolved, so no CLI installer or managed-run readiness is
claimed. Declared versions and byte matches are not architecture or dependency-closure evidence;
the legacy receipt's installed-toolchain refusal and all authority flags remain unchanged.

The subsequent managed scanner/solc handoff is now locally tested. The existing `ScannerRunner`
accepts the setup result's exact config and `host_tools` with an explicit existing isolation
backend. It rejects mixed configs and custom adapters before discovery, rebuilds fixed adapters
per run, and rechecks complete selected material/config at queued dispatch and after outcomes.
Primary host paths and Slither/Foundry compiler paths avoid ambient fallback; Foundry runtime clones
retain both paths. Disabled and image-side behavior, source custody and pin/refusal checks remain.
The 28-file compatibility matrix passes 1059 in 27.93s; all 40 new unit/local integration cases
pass in 4.18s. Final seven-file governance/documentation/objective/schema/inventory/CI checks pass
276 in 70.13s, including both new inventory regressions; all 42 new cases pass. Ruff, ten-file
formatting, strict mypy (244 files), canonical generation and governance pass. The local control
runs only trusted Python through the real runner/resource checks, not a real Semgrep scan, and its
execution evidence remains UNVERIFIED. No complete audit or installed toolchain is claimed.
The subsequent bounded isolation-launcher admission prerequisite is tested; AUTONOMY remains
PARTIAL. Identity is observed before policy/probes, rechecked around each invocation and retained
in the process-local seal. Launcher or policy drift refuses attestation; restored bytes need fresh
preflight. Failed resealing, including malformed identity, revokes old evidence. All six properties
remain mandatory. Final 21-file compatibility passes 697 in 18.93s; all 11 local integrations pass
in 1.07s with reviewed local permission, including the actual six-property system sandbox check.
The inspected loopback/backend rerun passes 49 in 39.95s. Earlier socket EPERM errors and an
inconclusive sandbox result remain historical and are not credited as passes. Sixty-seven new
cases pass across unit, local integration and inventory checks. Final seven-file governance/
documentation/objective/schema/inventory/CI matrix passes 278 in 71.53s.
Those checks preceded the managed Linux consumer. It is now implemented and locally tested:
ScannerRunner constructs selected pinned Bubblewrap automatically from prepared material when no
backend was supplied, requires a matching Linux/CPU declaration and retains pin admission through
all six probes. Each wrap reverifies full material and mounts only its closed directory read-only;
writable-private overlap refuses. Explicit backends stay explicit; no ambient fallback is added.
The 31-file compatibility matrix passes 1086 with six Linux-only INCONCLUSIVE cases in 30.18s;
final focused unit/native setup passes 48 with six INCONCLUSIVE in 2.48s. Existing real local/macOS
integration passes 11 in 1.00s. The six Linux kernel bodies did not run on this Darwin host and
remain required evidence, not mock passes. Fifty-seven new cases total: 51 pass / six unrun.
At that earlier closure, implementation was UNSELECTED and parent AUTONOMY PARTIAL; subsequent
compiler and formal handoffs are recorded below. Real Linux verification, independent trust, architecture/helper/
interpreter/dependency/image closure, atomic exec and an unattended audit remain unproven.
No provider, paid command, route selection, CI restart or managed readiness follows. Inventory
and canonical artifacts are reconciled. Final seven-file governance/documentation/objective/schema/
inventory/CI verification passes 281 in 71.68s; exact current state, operator and historical bindings
verify. Matrices overlap; none establishes the six unrun Linux kernel cases or a complete audit.


The existing compiler now accepts exact prepared Forge/Solc and matching smart-contract config,
requires fixed build arguments, forces explicit Solc with auto-detection off, and checks both
versions before compilation. Retained identities and full material/config are rechecked around
probes/build/outcomes. Source/private overlap and drift refuse; explicit backend, disabled/plain
and off-host Hardhat behavior remain. Final compatibility passes 851 with six prior Linux-only
INCONCLUSIVE cases in 22.37s. The inspected Hardhat boundary file separately passes 38 in 3.97s
with local-loopback permission; initial seven EPERM errors are not credited. All 57 new cases pass:
45 unit, ten real offline setup/trusted-Python controls and two inventory checks. Real Forge/Solc
compilation remains unverified: a fixed Forge exists, but no Solc was found in checked locations.
No tool was installed and no real compiler run is claimed. At that compiler closure, implementation
was UNSELECTED and parent AUTONOMY PARTIAL; formal-tool handoff was next. Inventory/records are reconciled; read-only
canonical verification passes, and the final seven-file governance/documentation/objective/schema/
inventory/CI matrix passes 283 in 70.49s. No private input, provider command, paid trial or CI restart.

The prepared formal-tool handoff is now locally tested. FormalRunner accepts prepared material
and matching formal config, rebuilds the eight fixed adapters, and uses explicit primary paths,
Halmos Z3 and Foundry-invariant Solc without primary PATH fallback. It preserves property/trust/
service prerequisites; preparing Certora CLI does not authorize service use. Bounded identities
and full material/config are rechecked around isolated probes, stream setup, launch and outcomes.
Version mismatch, byte/inode drift, custom adapters, config mismatch and path overlap refuse.
The same managed Linux factory is used when enabled and no explicit backend is supplied.
Twenty-file compatibility passes 525 with six prior Linux INCONCLUSIVE cases in 31.28s; the new
handoff/full inventory matrix passes 103 in 68.86s, including all 56 new cases (40 unit, 14 real
local trusted-Python controls, two inventory). Static/canonical checks pass. Final seven-file
governance/documentation/objective/schema/inventory/CI matrix passes 285 in 71.69s after terminal
state reconciliation. The optional full-pipeline expansion was interrupted after
542 passes, six skips and one already-fixed fixture failure in 544.07s, not a complete pass.
No real formal engine or Linux kernel execution, installed/interpreter/dependency/image closure,
atomic exec, service permission or unattended audit is inferred. At that formal closure, parent
AUTONOMY was PARTIAL and implementation UNSELECTED; the local invariant handoff was next. The active
plan/operator evidence, zero completed real audits and all false authorities remained unchanged.
No private input, route/spend decision, provider command, paid trial or hosted-CI restart occurred.

The prepared source-local invariant handoff is now locally tested. FoundryInvariantRunner uses
exact prepared Forge/Solc and matching config, checks pins before isolated versions, and uses the
prepared compiler directly outside writable generated source with auto-detection disabled. Detached
typed inputs and retained identities are checked around probes, private streams, launches, replay
and recursive minimization. Managed disabled mode does not probe; nonlocal mode, overrides, root
overlap and drift refuse. Existing legacy fork behavior is preserved, not exercised against a service.
Twenty-one-file compatibility passes 727 with six Linux-only INCONCLUSIVE cases in 47.25s; all 61
new cases pass (43 unit, 16 real offline trusted-Python process controls, two inventory). Static checks
pass and canonical inventory is reconciled without promoting any gate or authority. Final local invariant
seven-file matrix passes 287 in 70.36s. These controls do not prove actual Forge/Solc or Linux execution,
installed closure, atomic exec, complete managed-pipeline composition or an unattended audit.
At that local-invariant closure AUTONOMY was PARTIAL, implementation UNSELECTED, 42 unfinished;
prepared reproduction-tool consumption was next, using synthetic inputs and existing RPC refusals.
The exact latest operator evidence, active no-candidate plan, reported zero real audits and all nine
false authorities remain unchanged. No private input, provider command, paid trial or CI restart.

The prepared reproduction-tool handoff is now locally tested. ForkReproductionRunner binds exact
prepared Forge/Solc and matching config, isolated versions before target execution, explicit compiler
selection and retained identity/config checks through clean replay and result publication. Typed
inputs are detached; candidate identity, target/capability rules, root separation and the loopback
boundary remain enforced. Disabled/unacknowledged mode does not construct a backend or read RPC;
prepared chain/block pins are required. The managed Linux backend still denies network and refuses
fork reproduction. No fork endpoint is provisioned or actual engine/chain executed here.
Compatibility passes 759 with six Linux-only INCONCLUSIVE cases in 57.53s. All 67 new cases pass:
49 unit, 16 real offline trusted-Python controls and two inventory regressions. Static/canonical
checks pass; final reproduction seven-file matrix passes 289 in 71.71s. Controls remain
UNVERIFIED; complete installed closure, real engines/fork/Linux and unattended audit are not proven.
Parent AUTONOMY remains PARTIAL, current implementation UNSELECTED, 42 unfinished. Next connect
prepared tool material to audit-pipeline construction with unresolved admission gates intact.
Exact latest operator evidence, active no-candidate plan, zero reported real audits and nine false
authorities are unchanged. No private input, provider command, paid trial, CI restart or push.

### Previous 09:25 report and local repair evidence

The latest operator-supplied record reports unchanged totals (57 ledger entries / USD 0.68118684),
zero completed real audits, and a pre-credential development-transport refusal when endpoint-level
reasoning inventory is absent but the model-level inventory supports high. These are reported
observations, not independently authenticated accounting or authority to call a provider.

The automatic dependency-config handoff slice is tested and its AUTONOMY parent remains PARTIAL.
`V3-GOVSYNC-002` separated exact current report bindings from immutable September-4
history. Its earlier seven-file matrix passed 230 tests, including 53 new regressions; detailed
validation and limitations are in the worklog. `V3-DEVREASON-001` is COMPLETE: the local defect
is reproduced and repaired, with 1118 compatibility tests and 131 focused tests passing (overlapping
matrices), including 33 new regressions. Current inventory is reconciled without promoting any gate.
Terminal checks and the later operator-reported preparation verification are recorded in the
worklog. No paid request, independent private-input verification or completed audit is claimed.

The supply-path answer is now explicit: use the complete validated `candidate-<model-id-sha256>.json`
published by `models discover` as the existing `review-fixture --endpoint-snapshot` input. Do not
extract only the nested snapshot from ordinary discovery: model reasoning lives in the parent
payload and would be lost. The CLI now accepts the whole file and retains/revalidates those exact
model/endpoint facts at dispatch. Standalone snapshots remain supported when they contain the
required endpoint inventory or bound constrained-route model facts. `list-endpoints --json` is not
a review snapshot. No extraction script or newly issued live discovery/paid command is needed here.

The duplicate-route enumeration and durability-reference statements remain separate operator
observations. Do not infer duplicate tolerance, fresh route admission, private snapshot access,
lineage adoption, qualification or release. `V3-DEVTRIAL-001` stays queued and unselected; the active
plan has no candidate, retains V1, and has not adopted private V2. No credential or real-ledger
access, external enumeration or paid execution is requested by this follow-up.

## Real OpenRouter execution

Real provider tests require all of the following:

- explicit `MMAUDIT_RUN_REAL_PROVIDER_TESTS=1`;
- explicit operator-controlled `--secrets-env-file PATH`;
- an exact model allowlist and exact approved endpoint;
- an explicit `MMAUDIT_REAL_PROVIDER_PRIVACY_PROFILE=SYNTHETIC_BENCHMARK` profile;
- a numeric per-command cost cap within the aggregate remaining budget;
- a fresh absolute private JSON evidence destination beneath an existing
  operator-controlled directory;
- a committed synthetic local Solidity source scope for smoke and qualification;
- the exact frozen release-pinned policy supplied as `--qualification-policy PATH` for candidate
  campaigns;
- fallback routing disabled for certification.

Private production audits additionally require a pre-execution
`--learning-tenant-scope-id tenant-scope-<64-lowercase-hex>`. The operator provisions one opaque
value per tenant boundary; repository/output paths, run IDs, and source hashes are not tenant
identities. Terminal learning stays private, manifest-bound, and explicitly nonauthorizing.

The secret file is never target input and its contents must not be displayed,
logged, hashed, copied, or persisted.

The dedicated `models authenticated-runner` command does not use the test-harness
`MMAUDIT_RUN_REAL_PROVIDER_TESTS` switch. It requires the same
`SYNTHETIC_BENCHMARK`/ZDR/no-fallback policy, the explicit `--allow-code-egress` CLI opt-in,
three fresh singleton discovery registries, the exact frozen `--qualification-policy PATH`, and
same-process runner custody. Its `--preflight-only` mode deliberately stops before secret selection
or provider access. The completed provider-free runtime-admission mechanism additionally accepts an
explicit absolute private canonical legacy-v1.2 or current-v1.3 smoke bundle through
`--runtime-evidence-smoke-bundle PATH`; that input can affect only the exact three
route/profile/discovery bindings it proves and cannot grant qualification, campaign, runtime,
audit, selection, or release authority. Token evidence and usage routing must join the full
request-token-plan hash, while the preview must separately join the recomputed stable projection
hash.

Schema retry continuity is provider-free and explicit. The frozen retry-off base remains
`config/openrouter-qualification.toml`; a future authenticated-runner launch selects exactly three
same-route schema retries only by also passing
`--retry-continuity-config config/openrouter-authenticated-runner-retry-continuity.toml`.
`max_model_retries = 1` remains transient-only, the combined maximum is five attempts per logical
request, and first-attempt-only qualification scoring is unchanged. Current durable v1.2 evidence
and offline verification bind the full effective configuration, execution subtree, and attempt
capacity. These local controls grant no candidate, provider, campaign, qualification, audit, or
release authority.

Ordinary paid audits and their provider-free quotes use the separate
`--schema-validation-retries N` selection on `mmaudit run` and `mmaudit quote create`, with `N`
bounded from `1` through `31`. Omission keeps same-route schema retry off. A configuration with a
nonzero quota requires the exact same explicit CLI selection, and a conflict fails before paid
controls or quote inputs. `max_model_retries` remains transient-only. Current quote, acceptance,
budget, usage, and manifest-v1.3 evidence bind the exact split-policy hash; manifest/smoke v1.2 is
legacy retry-off replay and cannot substitute for current split-retry custody.

### Provider-free actor-model input

An actor model requires no provider access. An operator authors repository-relative JSON from
reviewed governance and economic evidence against `schemas/actor_model.schema.json`; model output
must not invent holders, admitted roles, constraints, or incentives. Start from the non-production
shape in `tests/fixtures/actor_model/synthetic_orchard_actor_model.json`, replacing every synthetic
assertion and evidence reference for the audited subject.

The file's `artifact_sha256` is the SHA-256 of compact, key-sorted UTF-8 JSON for the validated typed
payload excluding that hash field. Configure `[actor_model]` with its path plus matching
`expected_subject_id` and `expected_model_sha256`; add `expected_source_sha256` to pin the exact
source bytes. At run start,
only `valid_from <= run_started_at < valid_until` is current. Future, stale, invalid, or mismatched
evidence never calibrates findings. Set `required = true` when a non-current input must keep the
audit incomplete. Current input makes unresolved actor assessments a required quality gate even
when `required = false`.

### Current AUTONOMY offline dependency construction and receipt verification

The provider-free `managed build-dependency-snapshot` command builds the existing dependency
snapshot/config format from explicit local SHA-512-addressed npm archives and hash-pinned advisory
JSON. Its inputs are `--repo`, `--archive-root`, `--advisory-path`, `--advisory-sha256` and optional
`--verify-only`/`--no-color`. No credential, ledger, route, registry or package-execution input exists.
The archive store is separate from the target. Every detected Hardhat root in the bounded audited
workspace needs an exact npm v2/v3 lockfile; each supplied archive's actual digest must match its
lock entry before bounded inert unpacking. Duplicate/special/unsafe members and incompatible output
fail closed. Supplied advisory matches continue to be rejected by existing dependency preparation.

Output lives under the target's private `.mmaudit/managed-dependencies/<content-id>/` directory.
It includes the snapshot, nonauthorizing provenance and an exact `dependencies.toml` configuration
fragment. Completed repeats verify without overwrite; incomplete output is preserved, not reset.
No artifact claims the supplied lockfile is trustworthy or the advisory feed current or complete.
The existing `managed provision` runtime now consumes the dependency section explicitly selected in
the audit configuration. Its read-only verifier binds the exact detected Hardhat root/lock/package
sets, checks private owned directories/files (`0700`/`0600`), inert contents, package identities and
tree hashes, and refuses supplied advisory matches. Selected limits apply, capped at the builder's
maximum resource limits. Aggregate metadata checks detect changes across package observations,
including restored content; pre/post-publication checks reject later drift. Only a newly published
receipt is rolled back on detected drift, never a pre-existing historical one or dependency material.

New receipts identify setup policy v2; historical v1 receipts remain reproducible without relabeling.
Verified dependency observations do not resolve missing toolchain, fork, CodeQL or other inputs:
the receipt remains `REFUSED_INCOMPLETE`, every authority/readiness flag remains false, and the CLI
exits incomplete. This does not reauthenticate archive origin or prove feed freshness/completeness.
Observations are point-in-time, not a filesystem lock, and cannot replace audit-time verification.
Automatic dependency-config handoff is now available in the same `managed provision` operation:
provide all of `--archive-root`, `--advisory-path` and `--advisory-sha256` with the original audit
profile. It must contain no explicit dependency snapshot path/hash; conflicting pins and partial
source options are rejected before construction. No profile is overwritten and no source, provider
or permission is inferred. Limits use the tighter of the selected values and builder defaults.
Repeat with the same original profile and inputs. `--verify-only` cannot create missing dependency
material or a ledger. Failed construction stops before ledger creation or new receipt publication;
existing partial material and previous receipts remain untouched. Archive/advisory inputs cannot
overlap receipt output. Missing/invalid material remains incomplete, never readiness.

The combined runtime API returns a detached canonical effective config bound to its receipt;
real local tests prove that the existing dependency adapter consumes it without manual pin copying
and still rejects a supplied matching advisory. The existing receipt-only API and policy-v1/v2
receipt schemas retain their meanings. No full audit profile is written to disk by the handoff.
Managed setup now also applies declared pins to the existing compiler/scanner/formal consumer
fields for already selected roles, plus compatible selected container image/runtime fields.
One exact field map drives both conflict validation and derivation; a supplied conflicting pin
is rejected before dependency or ledger setup. The full effective configuration is revalidated,
with unchanged role/profile/engine selection and limits, then bound into the receipt. The public
pure `derive_managed_toolchain_config` API requires resolved roles by default; partial setup
retains unresolved-role refusals. It performs no lookup, download, installation or tool execution.
Existing fields required during config parsing (such as clean-fork Anvil pins or an explicitly
rootless profile's image) remain required; this is not a new raw-profile bootstrap format.
Synthetic bundle fixtures and matching/mismatching consumer observations prove propagation only,
not installed identity. Audit-time binary, version, isolation and source checks remain necessary.
Archive/feed distribution, advisory assurance, a complete managed execution path, installed-tool
attestation and autonomous-run readiness remain unfinished. AUTONOMY stays partial after this
tested slice. No paid operator command is issued by this guidance.

### Historical AUTONOMY Phase-2 local provisioning boundary

At the recorded September-3 boundary, the work unit closed only a bounded local-provisioning slice;
`V3-AUTONOMY-001` remains `PARTIAL`. The supported `mmaudit managed provision` interface takes
explicit `--repo` and `--output-dir` paths plus optional `--config` and `--verify-only` controls. It
is provider-free and loads configuration from the selected file with ambient configuration
overrides disabled. It hashes the bounded audited-workspace content inventory, subject to the
compiled exclusion domain, under retained local custody, but it does not select
or read an operator secret source and does not make a provider or network request. The repository
and owned mode-`0700` output directory must be absolute and disjoint.

The one implemented provisioning action is exact cost-ledger create-or-verify. The configured
ledger path must be absolute, have an existing private parent, and remain outside both the repository
and receipt directory. A durable private provisioning marker serializes creation and prevents a
deleted ledger and ordinary lock from being treated as a fresh budget. Without `--verify-only`, a
ledger is initialized only when no prior marker, state, or ordinary lock was observed; an existing
ledger is opened only when its immutable cap and identity are exact. `--verify-only` requires the
existing exact marker and ledger pair. The marker is retained on initialization failure, and its
identity is bound into verified receipt evidence. Both paths record a typed point-in-time snapshot,
including entries, active reservations, portfolio holds, and held value. They do not reserve,
charge, reconcile, clear, or otherwise alter an existing ledger.

The command emits a self-contained typed receipt containing the complete plan and reduced state.
The canonical JSON envelope is self-hashed and content-addressed. Repository custody is finalized
and the original digest is reobserved before any final receipt name is linked. The fully written and
synced private temporary file then uses atomic no-replace publication; an existing destination is
accepted only when its canonical bytes and typed envelope are exact. Rollback is identity-checked
under the cooperative publication lease; it is not an unconditional guarantee against a
lock-ignoring same-UID pathname replacement. The repository digest is point-in-time evidence, not a
promise that source remains unchanged after custody finalization. The receipt records
`spend_admission_evaluated=false`, `runtime_authority=false`, `managed_run_ready=false`,
`provider_or_network_accessed=false`, and `operator_secret_sources_accessed=false`. Repository
content hashing is evidence collection, not trust or execution authority.

The following Phase-2 surfaces remain unsupported by this slice:

- CodeQL database and query-suite creation or verification;
- dependency-snapshot materialization, target binding, and transitive-closure verification;
- pinned loopback fork-RPC provisioning, identity, and health verification;
- installed-tool discovery, exact executable pinning, transitive toolchain verification, and image
  side attestation;
- process, isolation, execution-evidence, runtime-admission, and spend-admission authority; and
- any managed audit run, candidate or route selection, benchmark, qualification, seal, promotion,
  or release.

Required unsupported inputs and unresolved managed-toolchain roles reduce to closed typed refusals;
unused optional inputs remain `NOT_REQUIRED`. The mandatory installed-toolchain refusal keeps every
current receipt `REFUSED_INCOMPLETE`, even when the cost ledger is created or verified exactly.

This provider-free capability grants no selection state. The active repository artifact is now the
canonical schema-v1.7 unavailable-candidate plan
`14566de1f7da5e4a769502bdd6a7e1ec6c0f193ed126c8fc85236f0851586fd3` (raw SHA-256
`4e7fff76ffb126a1cdf044cdfc889d79def96a29076aa11e3b42c7ef0ff9a695`), derived from exact
predecessor `bb3d60c3ff75ed2062b1ee68fe7b2011cf37ce860461b7d37eb10cd5faf7650f`. It selects no
candidate or candidate route, preserves the exact V1 profile and judge constraints, and explicitly
does not adopt the operator-reported private V2 successor. Codex did not invoke `mmaudit managed
provision` against operator-owned paths. Earlier current-state descriptions are historical at their
stated boundaries.

### Current PLANADOPT provider-free closure

The ticket-selection and unfinished-count statements in this subsection describe the 2026-09-04
closure. The primary Codex queue/worklog govern current development selection. The retained
plan, ledger, and zero-real-audit observations below remain unchanged by `V3-DEVCOST-001`.

`V3-PLANADOPT-001` is `COMPLETE`, provider-free, and nonauthorizing. The active plan records
`NO_ACTIVE_CANDIDATE_AFTER_REVOCATION` under state
`98941c3253ffe0f1aa88890b1c8b7fafb7d6575ee28ca31cb7724f14e784ceae` and exact matched
revocation-set digest `7c0118f5c170d46e6d2478bf92cbd83d1be6b426bda36dd57a6fc93e2ffd18c5`.
It contains no active revoked constraint. All durable authority flags remain false, the operator
ledger and `completed_real_audits == 0` are unchanged, and no replacement candidate was selected.
The retained exact V1 profile is
`00b33f3eff0ee7ac7710253c34786ce0a041ffe881baa4015dce0ed4f4b7ce82`.
Structural private-plan loading is not lineage authentication; any future repository adoption must
validate the exact predecessor and use a separately authenticated transition from this unavailable
state. Current and next tickets are `UNSELECTED`, PLANADOPT is last complete, PRICECAPCACHE is last
partial, and the combined unfinished count is `41`.

The latest operator-supplied record is 190,177 bytes / 3,377 lines at SHA-256
`215ea0f2f312b9674fb51285f6fdf758b166a0a998e2d2d621ac2da42f9a5f19`, latest
`2026-09-04T03:39Z`, at remote-resolved commit
`810ed7f32a9f39df104a6959e84b71c59966fb44`. It verifies the active schema-v1.7 null-selection
state and observes the deliberate authenticated-ancestry gate. No supported transition exists
today; it is deferred to a future separately selected bounded ticket. The operator should stop
probing this path, and this guide issues no command. The ledger remains 57 entries / `0.68118684`
USD and completed real audits remain zero. This evidence is operator-supplied, nonauthorizing, and
not independently authenticated by Codex.

**2026-09-07 local repair completion:** `V3-PLANANCESTRY-001` now completes the bounded,
provider-free ancestry mechanism. Exact issued-instance lookup, class/function/closure integrity,
single-endpoint roster narrowing, source/schema V1 parity, transitive repository I/O guards and
retained-descriptor validation/rollback have local regression coverage. The compatibility matrix
passes 785 tests. Current evidence and limitations are in `docs/codex_worklog.md`; historical WIP
warnings above do not describe the repaired code.

This is not permission to run a paid command or adopt a successor. The supported local construction
surface is `models emit-selection-plan-reactivation`, and it accepts only endpoints already in the
active catalogue. It cannot refresh that catalogue or admit the operator's reported
`amazon-bedrock/us-west-2` replacement by inference, upgrade V1, resolve price-cap expressibility,
or establish fresh route admission. No candidate is chosen here. The active schema-v1.7 plan still
has no candidate, private V2 is unadopted, and the recorded ledger and zero-real-audit facts are
unchanged. Bounded in-process checks are not a sandbox or crash-atomic publication. Completed
`V3-GOVSYNC-001` separates the current engineering record from historical results; its seven-file
177-test matrix passed, without a full-suite or external-result claim. The ensuing bounded local
`V3-AUTONOMY-001` provisioning slices are recorded in the worklog; the top follow-up section names
the current next ticket. No paid trial is selected here.

### Current engineering coordination versus historical evidence

Only `current_engineering_state` in `runtime_status.json` and `review_traceability.json` describes
today's engineering selection, queue count and current artifact bindings. Their surrounding fields
are explicitly historical, with every pre-existing value preserved by a pinned semantic digest.
Both worklog headers bind the exact current record. Timestamped entries and the separately labelled
September-4 pause header retain their historical scope; a former "next action" is not today's
permission. The operator-results file remains operator-supplied, not independently authenticated.

Use `make governance PYTHON=.venv/bin/python` for a read-only consistency check. It has no repair,
adoption, provider, credential or ledger action and runs before pytest in `make test` and `make check`,
including parallel make.
A changed operator result, plan, inventory or queue is a reconciliation failure until the current
engineering record is explicitly updated from that evidence. Do not copy old records into current
authority or change history to satisfy a check. A valid coordination record is not a qualified
model, completed audit, autonomous-readiness result, release seal or permission for paid execution.

Operator reports use newest-first level-two headings of the form `## YYYY-MM-DDTHH:MMZ` or
`## YYYY-MM-DDTHH:MM:SSZ`, optionally followed by a title. New entries go before the pinned original
September-4 entry, after the unchanged introduction. Each timestamp must be valid, unique and newer
than the following entry. At most 1,024 dated entries through the original anchor and 16,000,000
document bytes are accepted. The original introduction plus historical suffix must retain their
independently pinned digest, byte count and line count; changing current summaries cannot re-pin
that history. Later reports are bound to the current complete document, not an authenticated
append-only journal of all intermediate versions.

The latest entry must state one explicit ledger total using `Ledger unchanged at N entries /`
or `Ledger now at N entries /`, followed by the exact decimal USD amount in backticks and `USD.`.
It must also contain one explicit sentence: `` `completed_real_audits` remains `0`. `` or
`` `completed_real_audits` is `0`. ``. That sentence may begin a line (up to three leading spaces)
or follow another sentence ending in `.`, `!` or `?`. Spaces, horizontal tabs and a single LF or
CRLF line wrap between accounting tokens are supported, including trailing spaces around the wrap.
Blank paragraphs, section headings and hidden examples cannot join fragments into one fact.
Counts have at most 12 digits; amounts have at most 12 integer
and 18 fractional digits. Current metadata must match these reported values, including the exact
decimal string. New reported ledger totals may differ from historical totals; no private ledger
is opened or independently authenticated. This coordination record cannot credit a nonzero
completed-real-audit claim.

Code fences, indented code, block quotes and HTML comments are ignored as examples. Missing,
unsupported, repeated or contradictory recognized accounting statements fail closed; facts are
never inherited from an older entry. Reports and any commands inside them remain nonauthorizing
operator-supplied evidence. A fresh report alone does not select a route, adopt a plan or run a tool.

### Development-only estimated-cost policy (2026-09-06)

The operator explicitly approved `V3-DEVCOST-001`: a non-qualifying development policy that
accepts overspend risk. This approval does not adopt a candidate, authorize provider calls or
credential access, change the frozen hard-cost objective, or relax qualification/release gates.
The current bounded slice provides typed policy/estimate schemas, offline
`mmaudit development preview-cost`, and a reserve/reconcile adapter over the existing ledger.
See the README's **Development-only cost estimates** section for its provider-free interface.

Every preview requires `--accept-estimate-risk`, exact positive per-attempt/aggregate target
strings (at most USD 250), and absolute paths to bounded local snapshot/request JSON. It uses all
retained tier maxima, a default 2x multiplier, and at least the prompt price for cache-write
allowance even when quoted zero. Retries default off and their maximum is included in the estimate. One exact
operational ZDR endpoint, no fallback, no data collection, and text-only/no-tools/no-search controls
remain required. The preview neither reads secrets nor creates a ledger or sends a request.

The separate accounting adapter atomically refuses further development reservations while any
prior reservation or uncertain cost remains unsettled. It records actual charges, including
overages, before refusing further work. Its ledger target and estimates are not hard provider
ceilings: price changes or unreported fees can exceed them. Unknown costs cannot be cleared by
restarting or retried as free. All preview qualification/release/price-enforcement markers are
literal false; existing strict cost-proof types reject these artifacts.

The policy/preview/accounting ticket is complete. `V3-DEVRUN-001` adds the separate, locally tested
fixture-review transport described below; it does not launch a provider call during implementation.
No operator-specific paid-run command is issued, no new audit is recorded, and no active-plan or
runtime authority changes.
The strict `input_cache_write` admission refusal below remains in force; development estimates
cannot be used to remove metadata or manufacture qualification evidence.

### Development fixture-review transport (`V3-DEVRUN-001`)

The new `mmaudit development review-fixture` entry point is paid-capable only with explicit
`--accept-estimate-risk` and `--allow-code-egress` flags. It consumes a supplied endpoint snapshot
or complete validated discovery candidate JSON through the same `--endpoint-snapshot` input,
one of the two exact `ControlA.sol` / `ControlB.sol` fixtures under
`tests/fixtures/solidity/development_review`, an existing cumulative ledger, and an explicitly
selected credential file. All four paths must be absolute and distinct. The ledger cap must match
the aggregate target; this command never provisions, resets, or repairs accounting. Ambient secrets
and proxies are not selected. Consult `--help` and the README's **Pinned development fixture
review** section for the interface; this guide does not select a live model, endpoint, private
control path, run index, or paid command.

Request construction is fixed to the pinned abstract fixtures, native JSON Schema, high reasoning,
one exact ZDR route, no fallback, and no arbitrary prompt/tool/URL/code-execution surface.
The shared endpoint-first resolver permits model fallback only for absent endpoint efforts. Explicit
empty/unsupported inventories, missing model support, contradictions and modified metadata remain
refusals before credential/ledger access; a constructed prepared request is revalidated at dispatch.
Complete discovery files preserve parent model metadata that an unconstrained snapshot omits. The
input parser validates existing schemas and never repairs hashes or strips invalid provenance.
Published metadata, including serialized REAL labels, is not independently authenticated provider
execution or freshness evidence here. The command never refreshes metadata; future paid work still
requires current explicitly approved inputs, and none is selected in this repair.
Negative revocation checks remain active, including a recheck after reserving and before dispatch.
The shared fixture/shard routing repair accepts a canonical response model only when its exact
identity is bound by the complete supplied discovery metadata. Endpoint-only snapshots, including
constrained snapshots, remain request-ID-only; no response-authored alias is admitted. Both requested
and distinct canonical identities are checked for revocation. `requested` must match the request ID.
Explicit `false` BYOK evidence is required in router metadata or `usage.is_byok`; any true/malformed
marker refuses and absence in both remains unknown. These are local compatibility rules, not a
freshness, provider-authenticity or production qualification claim. See the README's linked primary
router/model/usage documentation for the underlying field distinctions.

New observations carry bounded `routing_evidence` when a bounded HTTP-200 body was decoded.
Named codes identify the failed routing condition; only metadata-bound identities and fixed
counts/flags are retained. Unknown strings, arbitrary router/pipeline/error text and raw headers or
bodies are not echoed; recognized secret-like or credential echoes are redacted. Old observations
may omit this field without inventing it. Fixture and per-shard observation schemas are extended;
the model response schema and outgoing request/estimate contract are unchanged.
The outgoing body must equal the exact bytes whose digest and estimate were reserved. Each command
makes at most one POST. Retry requires a separate explicit bounded attempt and settled prior costs;
unknown charges, pending reservations, and any reservation overrun block further requests.

Only local mocked-HTTP integration is validated in this ticket, including synthetic credential
loading and real temporary ledger writes. The normal adapter is fixed TLS/no redirects/no ambient
proxy/no HTTP retries. It bounds send/read time and response bytes. Routing metadata, exact model
and provider observations, token accounting, finish status, structured output, and source lines must
validate. Unsupported or missing metadata is incomplete, not a pass. Cost is parsed from exact
response JSON decimals and persisted even for rejected responses. Unknown or over-reservation cost
is retained honestly; cancellation never turns possible usage into a free retry.

The JSON result is a non-qualifying development observation. Exit success does not mean a secure
fixture or completed audit. Findings are hypotheses, not validated defects; the command does not
run them, reproduce them, shard a repository, choose judges, adopt a candidate, qualify a route, or
release anything. Provider billing and generation reconciliation remain separate evidence; a
forged/mock observation cannot become a production cost proof or trusted real usage.

Any first real fixture observation needs separately selected operator execution with approved
scope and spending controls. The active null-candidate V1 plan, unadopted private V2 plan, zero
completed-real-audit count and production admission constraints are unchanged. The ancestry repair
is already recorded COMPLETE; it does not select a candidate. The September-8 report records both
fixtures and both three-shard corpora as observed after the initial paid refusal; these remain
operator-reported, non-qualifying observations. The local synthetic routing matrix is not a replay
of private response bytes. V3-DEVTRIAL-001 is PARTIAL for missing fixture elapsed times,
uninspected exact private inputs and strict advisory ambiguity.

### Non-qualifying development measurement (`V3-DEVBENCH-001`)

The optional `--truth-manifest` for `development audit-corpus` selects the explicit v2 finding
contract and automatic source-pinned scoring. Use the exact paired development control manifest
under `tests/fixtures/solidity/development_audit`; no arbitrary truth or retrospective relabelling
of v1 results is accepted. The truth/plan binding is retained before dispatch and `score.json`
is automatically written alongside the exact aggregate observation, including incomplete runs
when output custody permits. Scoring failure refuses success while preserving retained costs.

The output parent must already exist as an unlinked private 0700 directory; no implicit recursive
creation is performed. The corpus, credential transport, explicit egress/estimate-risk selection
and existing cumulative ledger controls are unchanged. No new paid command is selected here.
See [metric definitions and limitations](../../development_benchmark.md): every duplicate,
advisory, missing shard and failed/unknown cost remains measurable; incomplete/empty quality
denominators are null. Truth is agent-constructed, public labelled and response-independent, not
external/non-model-generated qualification truth or an exhaustive safety oracle. Structural
matching cannot satisfy frozen-objective L, blind R, semantic validation or audit completion.

### Retained PRICECAPCACHE provider-free partial closure

**2026-09-06 diagnostic update (`V3-CANDROUTE-001`):** Constrained discovery now preserves
the selected price-cap algorithm and first controlled projection refusal after the existing
predicate codes. For the operator-supplied tiered-price shape, both V1 and genuinely selected V2
report `route contains a variable price without a provider cap: input_cache_write`; an unavailable
schedule instead reports `pricing schedule unavailable`. This is explanatory text, not new
admission evidence, an exhaustive list of all blockers, or permission to retry a provider call.
The local CLI regression verifies metadata-only mocked requests, no completion/ledger spend,
no discovery or registry output, unchanged input plans, and no credential-canary disclosure.

The current parser accepts the supplied tiers; a paired **synthetic-only** control passes cap
projection after omitting cache-write pricing from both base and tier metadata. Real provider
metadata must never be edited or filtered to manufacture that result. Neither the diagnostic
change nor repairing selection ancestry makes the recorded real route admissible. A fresh route
whose full charge contract is expressible, or an equivalent provider-enforced cap, is still needed
before real-route preflight and qualification. No new live inventory or operator command is issued.

`V3-PRICECAPCACHE-001` is `PARTIAL`, provider-free, and nonauthorizing. V3 remains a
reserved policy identifier, but the trusted `project_provider_price_cap` boundary rejects it
unconditionally before discovery proof, registration, cost preview, reservation, or provider
egress. V1 and V2 behavior remains preserved, including V1/V2 refusal of
`input_cache_write` even when the observed value is zero.

No supported successor derivation or CLI path emits a V3 plan. The default-off V1→V2 successor
remains available and unchanged only from an exact historical V1 predecessor; it is unavailable
from the current schema-v1.7 active plan without a separately authenticated ancestry transition.
V2→V3 derivation fails closed. Preview schema `1.3` and
durable pricing-attempt schema `1.2` have been removed; supported preview versions stop at `1.2`
and supported pricing-attempt versions stop at `1.1`. No V3 preview or attempt can be published,
adopted, or used for transport.

The requested dominance rule cannot provide the ticket's provider-enforced safety property.
OpenRouter's
[prompt-caching documentation](https://openrouter.ai/docs/guides/best-practices/prompt-caching)
describes provider-specific cache-write prices, while its
[provider-routing documentation](https://openrouter.ai/docs/guides/routing/provider-selection)
documents `max_price` ceilings for prompt, completion, request, and image only. It exposes no
request-bound cache-write or total-cost cap. A metadata-time cache-write-to-prompt comparison is
therefore non-atomic: pricing can change between metadata refresh and POST without violating the
transmitted prompt ceiling, and reconciliation observes an overage only after spend.

Independent review also reproduced a second historical schema-`1.2` understatement in which both
prompt and cache-write units were lowered and the artifact resealed. That format is not repaired or
accepted as authority; it is neutralized because V3 preview/attempt publication and transport are
now unreachable.

The active schema-v1.7 plan retains the exact V1 price-cap profile. The operator-reported private V2
plan remains unselected and unadopted, and its prior metadata-only refusal did not exercise V3. No candidate, route, command,
run index, provider/network action, configuration, retry, ledger, completed-audit count,
qualification, runtime, release, or other authority changed. Resume only if a provider supplies a
request-bound cache-write or total-cost cap, or an equivalent enforceable atomic contract.
At the historical pre-PLANADOPT committed boundary, remote-resolved HEAD
`ad013aeeb4ea754ec7f0c751175e4badd5fc95b2` added only queued, unselected, and unstarted
`V3-PLANADOPT-001` over operator-result commit
`c8a46b9b53a77fcdd4241d8fadeecbebc876346d`. That boundary is not current: remote-resolved HEAD
`810ed7f32a9f39df104a6959e84b71c59966fb44` now records the operator's PLANADOPT verification,
while the current worktree completes PLANADOPT locally with no current or next ticket and `41`
unfinished.

### Historical PRICECAPCOMP campaign-admission boundary

At that historical boundary, the then-current operator-supplied record was 187,993 bytes / 3,335 lines at SHA-256
`63bfe90b281a6382b921c63669e817e65220c81b629fc75a7cb35b3f3e429d5b`, latest
`2026-09-03T22:31Z`, at remote-resolved commit
`c8a46b9b53a77fcdd4241d8fadeecbebc876346d`. It reports that the operator emitted private V2 plan
`96cc5301111c7b60cf9b8235870d8051e2ece737c80dc5321662a1f6adfa3aea` and used it for one
metadata-only constrained discovery. The same
`PRICE_CAP_NOT_EXPRESSIBLE,PRICE_CAP_PROOF_UNAVAILABLE` pair remained, and the ledger stayed at 57
entries / `0.68118684` USD. This evidence is operator-supplied, nonauthorizing, and not independently
authenticated by Codex. It does not adopt the private successor, select its route or candidate, or
replace the active repository V1 plan; no further command follows.

The immediately preceding record is 185,462 bytes / 3,287 lines at SHA-256
`d502c61a2525a2bdcc2e7efd79b115821af60f604e715d7be7ccd51b02983ab1`, latest
`2026-09-03T21:03Z`. Commit `dd141cd580c2a012a6efdef26213d3a727b68041` is remote-resolved;
the record remains nonauthorizing and was not independently authenticated by Codex. Its newest
entry accepted that every then-completed operator retest used the active V1-derived profile and did
not exercise V2, then asked whether a real run could select V2. At the historical 21:11
reconciliation boundary no supported selection surface existed; at 21:18 the same ticket reopened
because typed direct construction did not meet explicit-selection acceptance. That gap was later
resolved.
`V3-PRICECAPCOMP-001` is `COMPLETE`: the supported provider-free command surface is
`models emit-selection-plan-successor --upgrade-price-cap-profile-v2`. The flag is default-off;
omission preserves exact V1/default bytes. It accepts only an exact V1→V2 transition, supports a
same-route successor, and rebuilds the shared profile plus every resulting candidate, PRIMARY, and
REPLAY constraint. This capability does not select or activate a plan: no successor artifact was
selected or inspected, no live command or route retest is requested, and
`input_cache_write='0'` remains independently uncapped and blocks xAI admission.

The prior exact `2da419f85a9f6b2d087c1f75dca4247ef91d8fcf23005c719644de5830238b21`
record (182,652 bytes / 3,240 lines, latest `2026-09-03T20:43Z`, remote-resolved commit
`b9ea875e70d865b50c1eb847e39ed767162c9c6b`) remains historical evidence of the credentialed
metadata-only V1 constrained-discovery retest: the xAI route returned
`PRICE_CAP_NOT_EXPRESSIBLE,PRICE_CAP_PROOF_UNAVAILABLE`, produced no snapshot, and left the
57-entry / `0.68118684` USD ledger unchanged. Its possible flat-only explanation was an operator
question, not a root-cause claim; local inspection showed discovery is schedule-aware and the
quoted refusal is refresh-only. The earlier `2026-09-03T14:30Z` entry remains the exact correction
that retracts the key-order cause: direct construction bypassed provider-facing ingest, which
already sorts raw tier keys, and a provider-free 120-permutation full-ingest assay yields one
pricing hash and one snapshot hash. `V3-PRICEKEYORDER-001` remains
`WITHDRAWN_OPERATOR_ERROR` and must not be implemented.

That historical local provider-free closure marked `V3-PRICECAPCOMP-001` `COMPLETE` without reissuing
that live discovery. An opt-in V2 route profile may retain nonzero `web_search` pricing only when a
self-hashed immutable envelope proves the exact emitted request has zero reachable search units and
zero search cost. Search/tool/plugin fields are prohibited, the component is omitted from provider
`max_price`, and the same profile/envelope is joined through discovery, tier-wide projection,
registration, preview, refresh/durable evidence, smoke/runtime evidence, and the frozen provider
egress callable graph. The active plan remains byte-identical V1. The recorded xAI route remains
unselected and inadmissible because `input_cache_write='0'` still lacks an enforceable cap,
dominance invariant, or immutable disable control. The default-off successor option validates only
an exact V1 predecessor, rebuilds all role constraints, and rejects repeat V2, downgrade,
drift/tamper, non-boolean input, revocation, and stateful predecessor substitution before publishing
a fresh mode-`0600` output. This local capability authorizes no command, route, qualification,
campaign, audit, or release. The latest operator retest exercised the private V2 successor only for
metadata discovery and reproduced the same two refusal codes. It neither adopts nor selects that
private artifact as the repository's active plan, which remains V1, and it does not convert the
operator's earlier flat-only question into a finding. Provider-free call-graph inspection answers
that question: constrained discovery canonicalizes and retains the tier schedule, while the quoted
flat-only refusal belongs to a separate refresh helper. The shared public failure pair cannot
distinguish V1's two component refusals from V2's remaining cache-write refusal. No further live
route retest is currently requested and no active successor is selected.

At its `2026-09-03T11:27:12Z` boundary, `V3-PRICECAPTIER-001` closed `PARTIAL`, provider-free,
and nonauthorizing. Exact Decimal maxima
across the base and every ordered inherited partial tier are bound to the complete schedule and cap
proof. Later applicable tiers win; cache-read dominance is enforced per effective state; and provider
cap, request-cost, reserve, spend, and reconciliation use the same deliberately conservative maximum.
Flat-route bytes remain unchanged. Unsupported or unavailable schedules fail
`PRICE_CAP_NOT_EXPRESSIBLE` and `PRICE_CAP_PROOF_UNAVAILABLE`, while nested numeric override lexemes
fail closed. The recorded xAI maxima are prompt `0.0000044` and completion `0.0000132`, but
`input_cache_write='0'` and `web_search='0.01'` remain independently uncappable. Refresh and live
preflight were flat-only at that historical boundary, so the primary route/preflight criterion was
unmet and no route was selected.

`V3-MODELREFRESH-001` now closes a provider-free tier-schedule continuation as `PARTIAL`.
Refresh source/snapshot evidence, deterministic drift, durable pricing route and attempt evidence,
endpoint registration, and live preflight retain the exact ordered schedule and join its complete
digest. Provider caps and cost custody use the same conservative maximum; threshold-only changes are
detected even when maxima match; unavailable, mismatched, and tampered schedules fail closed; and
flat-route bytes remain unchanged. This grants no route admission: the independent
`input_cache_write='0'` and `web_search='0.01'` constraints remain, and successful current provider
evidence, the stock live authority quartet, automatic real benchmarking, lineage re-evaluation,
qualification, and promotion are absent. No terminal full-suite credit is claimed.

`V3-PRICEOVERRIDES-001` is `COMPLETE`, provider-free, and nonauthorizing. Bounded ordered tier
entries are recursively validated and retained; exact thresholds and nested canonical decimal
prices, strict `>` threshold inheritance, complete-schedule hashing, endpoint binding, and exact ZDR
schedule equality are enforced. Flat endpoint evidence remains byte-identical. Tiered cost
projection is explicitly `unavailable`; request preview, endpoint registration, identity sealing,
and flat refresh refuse conditional pricing rather than flattening or silently dropping a tier.
The full synthetic five-endpoint `models discover` regression accepts the recorded shape and then
reports `PRICE_CAP_NOT_EXPRESSIBLE` and `PRICE_CAP_PROOF_UNAVAILABLE`. Thus
`input_cache_write='0'` and `web_search='0.01'` remain independent unresolved provider-cap
constraints; the sole-blocker claim and route admission remain unproven. This does not prove a sole
blocker or route admission.

`V3-PRICEFORM-001` is `COMPLETE` as the decision-only ordinary-JSON custody refusal; its closure
changes no code, configuration, retry behavior, route, or authority. `V3-PRICELEXEME-001` is
terminal `PARTIAL` provider-free defense-in-depth: its route-specific numeric-price premise is
superseded and it
is nonblocking for the xAI route. Pure Python cannot
retrospectively detect hostile same-interpreter reflection fully restored before a trusted lookup;
every observed mismatch is permanently revoked, and arbitrary closure reflection is outside the
trusted-interpreter boundary. The focused live-shaped full-`models discover` raw-numeric-price case
passes `1` in `0.76s`; the adjacent price-decoder/endpoint/discovery/OpenRouter/CLI matrix passes
`793` in `17.73s` with two known warnings. Scoped Ruff passes and strict mypy is clean across `233`
source files. Trusted execution evidence remains `MOCK`; exact constrained-route identity,
normalized pricing, multi-ZDR custody, and MOCK-to-REAL refusal are asserted. The interrupted broader
CLI/pipeline attempt remains at `111` passes in `193.54s` and has no pass or full-suite credit. This
guide authorizes or issues no live command.

`V3-TESTQUALITY-001` is `PARTIAL`, provider-free, and nonauthorizing after its shared process-local
revocation-lease race slice. One PID-bound domain serializes run, observation, campaign, and score
authority reads and registrations plus handoff replay. Composite layers revalidate exact local
seals after dependency release; decisive scoring uses seal-SHA-matched immutable schema snapshots,
binds authenticated and canonical plan hashes, and revalidates exact live sources at registration.
Callbacks defer beyond the outer lease, partial and `BaseException` paths exact-remove without
masking the primary exception, and fork paths refuse before inherited locks. The result remains
comparison-only and noncrediting; same-interpreter reflection, asynchronous-exception micro-gaps,
and the post-linearization return boundary remain explicit. It does not authorize an independent or
portable cleanup receipt, a current REAL statement or campaign receipt, portable same-UID disposal,
a sealed backend, a production plan generator, a REAL mutation run/kill artifact, or production
campaign authority. `V3-TESTQUALITY-001` remains independently `PARTIAL`. Candidate requests still require
`effort=high`; ordinary binary-float or
missing-capture price custody remains inadmissible; the conditional
`x-ai/grok-4.6=amazon-bedrock/us-west-2` route remains unselected and not currently proven
admissible. The active plan, retry configuration, 57-entry / `0.68118684` USD ledger,
`completed_real_audits == 0`, and every provider, runner, candidate, route, command, campaign, run
index, selection, qualification, audit, and release authority remain unchanged or false. AF7 remains
historical evidence at its exact `2026-09-01T04:49Z` boundary. The final PRICECAPCOMP selector
validation passes `120` focused tests and `1,328` affected tests with two known warnings;
release-schema, autonomy-inventory, and refresh-schema validation passes `79`; canonical
generation, scoped Ruff, strict mypy over `233` source files, and diff integrity pass. The
regenerated autonomy inventory hashes are raw
`fcecc2351ab9d5fd7a0e6c0289e9daeb7f6ec83cebfd00552948e9b090617c73`, self
`0f575febde00869a20192214a40af87e7196660a8e82b5810e8beefb45caad28`, discovery
`bc80c98c469dc9cce61af880c8849e8387143ad1e3fb7d853d4eca8e9205e340`, universe
`3f41a8b19a0e7547bc619f1405692e19a05e62d9662471f10736d79a2d619389`, and schema raw
`88790b6d1d02fff4f2a39ff0bc0c023d062459e6d19d594a1c93cee03871c6c7`; counts are 3,941 sources,
3,944 occurrences, 3,892 gate sources, 49 non-gating controls, and 337 completion entrypoint
parameters. No terminal full-suite credit is claimed.
At that boundary, `CURRENT_TICKET` was `UNSELECTED`, and no next local ticket was selected;
`V3-PRICECAPCOMP-001` is the last completed ticket, `V3-MODELREFRESH-001` is the last partial
ticket, `V3-PRICELEXEME-001` and `V3-CANDROUTE-001` remain partial, and the combined queues retain
`40` unfinished tickets.
`V3-PRICEKEYORDER-001` is `WITHDRAWN_OPERATOR_ERROR` and must not be implemented. Current and next
tickets were unselected at that boundary. The later operator-reported private V2 successor remains
unadopted and unselected; the active repository plan remains V1.

The historical `2026-08-30T19:00Z` credentialed metadata-only schema-v1.0 survey covered 12 models
and 112 endpoints, found zero endpoint-level reasoning-effort inventories, and exposed that the
diagnostic omitted model-level and admission-effective reasoning inventory. Local schema v1.1
corrects that gap and has not been exercised live; no repeat external command is required. The prior
15:18 plan-successor result is retained as historical evidence. The local emitter now has an
explicit schema-v1.6
`--refresh-endpoint-inventory` path that stages one previously unlisted endpoint as
`OPERATOR_STAGED_UNVERIFIED`, binds its predecessor inventory, requires later constrained discovery,
and grants no authority. Omission remains fail-closed. This local mechanism does not select or prove
a current real route, and no external action or command is current. Fresh operator evidence for a
current real route is required before `V3-CANDROUTE-001` can resume. Hand-editing the hash-pinned
input is not an approved workaround.

`V3-ENDPOINTLIST-001` schema v1.0 was exercised by the operator at `2026-08-30T19:00Z`: credentialed
metadata GETs enumerated `112` endpoints across `12` models, found zero endpoint-level
reasoning-effort inventories, issued no completion, and left the `57`-entry / `0.68118684` USD
ledger unchanged. The survey exposed that v1.0 omitted the model-catalog fallback and exact
effective reasoning value used by admission. The corrected v1.1 reference surface
`mmaudit models list-endpoints --model EXACT_ID [--json]` now reports separate model, endpoint, and
admission-effective reasoning inventories plus model/endpoint structured-output facts. Its shared
endpoint-first resolver preserves explicit-empty veto and exposes unavailable or contradictory
metadata. It remains metadata-only, self-hashed, ledger-free, and nonauthorizing. Codex validated
the correction only with local synthetic metadata and did not perform or authenticate the live
survey; the earlier v1.0 output does not validate v1.1. No repeat external command is required.
The survey produced zero admissible candidates, `V3-CANDROUTE-001` stays `PARTIAL`, and the active
plan remains unchanged.

The immediately preceding entry reports fresh r23 discovery, paid smoke index 22 (`0.04395915` USD;
bundle
`29702a02f52626deca38ff36401eb3cb7bb4602f07677881760ad26ba40df5d4`, verified
`VALID / NONCREDITING / NONAUTHORIZING`), and `FULL_CAMPAIGN_ADMISSION` satisfied for one launch.
The 24-case campaign then failed closed after roughly 24 logical requests and `0.20264508` USD: nine
schema-invalid responses became `SCHEMA_VALIDATION_FAILED`, 15 completed response identities were
unbound, and terminal candidate provenance was rejected. The operator reports all 24 new ledger
entries reconciled, 57 entries / `0.68118684` USD total, and zero completed real audits.

The corrected provider-free mechanism remains `COMPLETE` under `V3-RUNTIMEADMIT-001`, but that one
failed launch creates no reusable admission or future authority. Codex did not read the private
registries, bundle, campaign, or ledger. The frozen DeepSeek/`parasail/fp8` candidate is non-runnable;
the operator-reported successor sweep found zero admissible replacements, and its artifact is neither
active nor independently inspected by Codex. `V3-CANDROUTE-001` remains `PARTIAL` after its local
provider-free mechanism slice; no real replacement is selected.
The historical launch did not retry schema-invalid output on the same route;
`max_model_retries = 1` was and remains limited to transient network/status retry. The
operator-selected schema retry is now implemented and explicitly selectable provider-free as
described above, but it was not engaged in that launch and does not rehabilitate its rejected
candidate or evidence. There is no current operator command, run index, future FULL admission,
provider launch, qualification, or release authority.

## Endpoint-aware token planning

Every model request requires frozen endpoint metadata for its exact model and
approved provider routes. The conservative intersection of those routes supplies
the context, prompt, and completion limits; missing or incompatible limits fail
preflight.

Operators configure a usable-input fraction between 65% and 75%. mmaudit applies
that fraction only after reserving completion and reasoning capacity, then
reserves system, schema, protocol, and request-specific workflow overhead before
allocating source context. Configured reserves are floors, not permission to
ignore larger measured material.

Without an exact endpoint tokenizer, source selection uses a deterministic token
estimate while final request planning treats the complete UTF-8 prompt envelope
as a conservative upper bound. The source-token ceiling is therefore distinct
from the serialized context-package byte limit. Large metadata is reduced or
omitted with typed evidence before it can crowd required source out.

Visible output is allocated explicitly among findings, per-surface coverage, and
summary. Every category has a positive floor, coverage grows with the requested
surface count, and an infeasible allocation is rejected before transport rather
than silently reducing coverage.

Endpoint-capacity, context-plan, global-token-budget, and cost-budget
failures remain distinct. A rejected request may retain a self-hashed diagnostic
snapshot of whichever route, prompt-category, output-allocation, and omission
facts were measured; unavailable components remain explicitly unavailable.
Diagnostic snapshots contain no raw prompt or source and always record that no
provider request, reservation, or review credit was created. A preflight
rejection never counts as provider execution or substantive model review.

## External engines and isolation

Certified maximum assurance remains fail-closed until every mandatory engine and a
digest-pinned approved rootless isolation backend execute with real evidence.
Unavailable integrations must retain exact non-secret operator installation or
configuration instructions when their tickets begin.

### Trivy offline vulnerability database

The typed operator preparation step is
`prepare_trivy_offline_vulnerability_database`. Trivy 0.72.0 exposes this bounded
one-time network-enabled preparation command:

```text
trivy image --download-db-only --cache-dir <absolute-operator-controlled-cache-dir> --no-progress
```

Run that command only in an explicit operator-controlled preparation phase, never
inside target analysis. The audit phase remains offline.

The current adapter deliberately uses a fresh private per-run cache and does not
yet expose or stage an approved prepared-cache path. Consequently, running the
command against an unrelated cache does **not** unblock mmaudit today. Until a
future typed configuration binds, validates, and stages that exact prepared cache
read-only into the isolated scanner workspace, Trivy reports
`UNMET_PREREQUISITE` and does not earn scanner-completion or maximum-assurance
credit. No operator credential or target-controlled cache is accepted.

### Hardhat pinned-fork execution

`V3-FORKSUITE-001` cannot credit real Hardhat execution on the current host. A
future operator-authorized integration requires:

- an installed approved rootless Podman or Docker backend;
- an approved digest-pinned image containing the pinned Node.js, Hardhat, test
  runner, and machine-result reporter toolchain;
- REAL process and image-identity attestation bound to the emitted run;
- network-none container execution with a narrowly scoped Unix-socket,
  read-only JSON-RPC bridge to the exact operator-configured loopback fork
  endpoint;
- read-only audited source, disposable output and home directories, no host
  credentials or container socket, and bounded CPU, memory, process, output, and
  runtime limits.

Neither `podman` nor `docker` resolves on the current trusted host PATH and no
approved image digest is configured. The adapter therefore remains fail-closed
as `UNAVAILABLE`; broad container networking or host-loopback access is not an
acceptable substitute.

`V3-HARDHAT-001` now supplies the safe local contracts needed before that external
integration: pinned reporter source plus separate schemas, a non-crediting
two-phase request protocol, an owner-only AF_UNIX read-only RPC listener, a bounded
in-container raw relay, a `--network none` wrapper, fixed in-image executable
tokens, and a process-local lifetime seal that revalidates the exact backend,
bridge, PID, private directory, socket, endpoint, policy, and pinned state. Closing
the seal, stopping or replacing the socket, copying the backend, or changing any
bound identity invalidates command construction. This still grants only
`UNVERIFIED` command-construction authority.

The current trusted host resolves Node.js but not `hardhat`, `podman`, or `docker`.
The local protocol test therefore uses a handcrafted Node/EventEmitter reporter
process explicitly marked `MOCK`; it is not Hardhat/Mocha execution. It does not
prove `.only`, callback-less pending tests, runtime filtering, phase-one body
non-execution, relay-to-test attribution, monorepo project roots, container exit/
output custody, or image-side executable identity. JSX/TSX source snapshots are
deliberately rejected until a real syntax-aware parser exists. A real integration
must supply and attest all of those missing facts; no filename, host path, process
double, or serializable observation may substitute for them. Constructed container
argv is replayable and receives no authority of its own: the eventual executor must
retain the opaque live binding and revalidate it immediately before spawn and after
process completion.

The independent `supervise_hardhat_phase_process` primitive now captures an already-admitted
inventory or test command supplied by a trusted executor. Its launch tuple and explicit scrubbed
environment are bounded; it never inherits ambient environment or resolves a runtime through PATH.
The workspace and output must be disjoint direct paths. Output and its parent must be owned
mode-0700 directories, with the parent outside the child-writable mount. The fixed report name is
`hardhat-report.json`; an existing report or output claim refuses the phase. A private exclusive
claim is retained outside that output directory even after failure, so retries need fresh output.
Do not remove the claim to reuse evidence.

Anonymous stdout/stderr pipes and the report share one retained-byte ceiling. The phase deadline
includes startup, draining and report capture; separate bounded process-group cleanup runs on
normal, failure and interruption paths. Residual descendants invalidate an otherwise successful
exit. Stable no-follow, single-link report reads occur after cleanup, and request/root/host-runtime/
claim identities are rechecked before detached bytes can return. A complete capture is not a
passed test: execution credit and runtime authority are always false, and the raw report is not
semantically authenticated here. Parent capture ceilings are not container disk or resource quotas.

The supervisor integration controls exercise real finite Python child processes, not Hardhat/Mocha or a
container. They cover exit failure, time/output limits, binary streams, missing/linked reports,
leftover descendants and cleanup. The primitive is not wired into the production Hardhat adapter.
Retaining/revalidating the live backend binding, image-side identity, real container teardown,
per-test RPC attribution and actual Mocha source/selection behavior remain separate executor
requirements. The adapter therefore stays `UNAVAILABLE`; no host runtime
observation or locally passing capture test substitutes for those missing facts.
This slice passes all 47 new checks, 134 offline compatibility and 305 terminal consistency
checks (overlapping groups), plus Ruff, formatting, strict mypy and canonical verification.
HARDHAT remains PARTIAL. The subsequent capture-to-protocol join is now locally tested below;
its final compatibility and terminal consistency checks are recorded in the worklog.

`prepare_hardhat_test_phase_from_capture` accepts the exact inventory request/capture, current
source root and `SmartContractsConfig`, plus the same optional repository exclusion root. It checks
the committed phase-specific reporter-schema digest, reporter/version/request pins, exact suite
policy hash, seed and limits. Stable source binding and selection construct the returned
`HardhatPreparedTestPhase.test_request` automatically. Retain the whole prepared value: its original
inventory bytes, source seal and selection are needed for phase-two consumption. This value has
no command-admission, runtime or execution authority; constructing or copying serialized data cannot
recreate the existing process-local source seal.

`consume_hardhat_test_phase_capture` takes that prepared value, test capture and the current source/
config. It reparses retained inventory and reconstructs source selection before and after parsing
the test report, verifying the original source seal and exact two-phase joins. Root identity is
retained across consumption even if an identical tree is substituted. Captures must be terminal,
finite, bounded and phase-matched; timeouts, signals, missing output and failed cleanup refuse.
Reported failures require a positive normal exit, while pass/skip-only reports require exit zero.
Consistent failing and skipped observations stay visible without becoming execution or safety
credit. Both captures share the configured retained-byte and recorded-duration ceilings.

Eight real local Node/reference-reporter controls exercise this full connection, including matching
failure/skip reports, contradictory exits, stale request hashes and changed source. The controlled
EventEmitter is explicitly a test double: it loads no target JavaScript and runs no Hardhat/Mocha.
This join validates observations, not their authenticity or independent per-test resource
attribution. It does not measure the inter-phase gap or enforce a running container's total lifetime.
The next bounded executor slice must retain/recheck the live backend binding and independently
supervise the complete two-phase deadline and teardown; production execution remains refused.
The capture/protocol slice passes 84 new tests, 219 eight-file compatibility and 306 terminal
consistency checks (overlapping groups), plus Ruff, formatting, Node syntax, strict mypy and
canonical verification. No full-suite, actual Hardhat/Mocha or unattended-audit result is claimed.

The phase-layout prerequisite is now locally tested. `wrap_hardhat_phase` binds an exact typed
inventory/test request to the live backend image declaration, effective capability, bridge policy
and pinned chain/block observation. It requires the exact retained binding handle before and after
construction; an old closed handle cannot borrow a newly issued seal on the same backend. Source
directory and private-root identities are also rechecked. Source-content authenticity and executable/
reporter digest attestation remain separate executor/protocol requirements.

Each request claims a fresh `hardhat-<phase>-<request_sha256>` directory under the bridge-private
root. Only that phase's `container-output` is writable in its constructed container command.
The common source and socket are read-only mounts; host-side `container-runtime` holds the phase's
exclusive seccomp file and CID path. Use the returned `HardhatPhaseCommand.private_dir` as the
existing cleanup method's target, not the common bridge root. Failed or used phase roots remain;
do not recycle them. No directory or prior report is automatically deleted. Paths into another
phase are rejected and supervisor output claims stay outside the child-writable mount.

The 60 new layout unit/local integration cases and one inventory case pass. Owned loopback/Unix
servers exercise actual binding lifetime; two fixed finite Python captures consume both layouts.
Cleanup routing is tested with a mocked runtime, not a real container. Constructed commands have
no image/launch authority and the backend stays UNVERIFIED; the production adapter stays
UNAVAILABLE. The remaining bounded executor work must retain binding ownership across both phases,
enforce total elapsed time through gaps and cleanup, and separately establish image admission and
actual container teardown. No private input, credential, provider/public-RPC or hosted-CI action
is selected.

`capture_hardhat_two_phase_execution` now supplies the owned two-phase observation lifecycle.
The caller transfers an exact live backend/bridge/binding only after their process-local join
verifies; a mismatched bridge cannot make the driver close unrelated resources. Startup before
that handoff remains the caller's preparation. Once ownership transfers, every return/error path
closes the binding and stops the bridge, with a clean matching endpoint-free snapshot required
before a result. Source, request, configuration and protocol observations are rechecked after
shutdown. Failed ownership or phase reuse never starts another process.

This low-level API requires a trusted programmatic `HardhatPhaseBoundary`, never a value supplied
by target configuration, a model or a serialized readiness flag. Its image-command builder feeds
the existing unverified phase wrapper. Its context manager must independently admit each launch
and perform bounded container/resource finalization, including cleanup of a failed context entry.
The driver always owns parent capture and invokes finalization after successful entry; finalizers
cannot suppress or replace an original capture error, interrupt or exit. No production boundary
implementation is supplied by this slice. Passing a callback is not image attestation, qualification
or permission to execute repository code, and the production scanner remains UNAVAILABLE.

The supervisor's optional absolute deadline and retained-byte allowance only narrow the sealed
request; request/report hashes are not rewritten to reset a budget. Phase two receives remaining
bytes, not a fresh full allowance. Actual children are stopped when a shared bound is crossed.
Monotonic checks include preparation, protocol work, inter-phase gaps, finalizers and bridge
shutdown; late results refuse. Emergency cleanup still runs after expiry. Trusted synchronous
Python callbacks are not preempted, and this is not proof of a container's hard lifetime or of
independent per-test resource attribution. Those remain requirements of the real admitted boundary.

All 69 new checks pass: 54 unit cases, eleven actual owned-bridge/fixed-Node-reporter integrations,
three finite-Python shared-bound controls and one inventory case. Compatibility passes 219
Hardhat checks and 98 bridge/layout checks (groups overlap with other recorded runs). No actual
Hardhat/Mocha, container, model/provider or public-RPC call occurs. The driver returns noncrediting
observations only; supported Linux isolation, best-in-class performance and an unattended audit
remain unproven. Current coordination and terminal results are recorded in the worklog.

The exact rootless-cleanup prerequisite now replaces inspect-error-as-absence. The typed
`cleanup_rootless_container` result distinguishes `NO_IDENTIFIER`, `ALREADY_ABSENT` and `REMOVED`.
Only a successful empty full-ID-filtered listing can establish the selected runtime's reported
absence; a present reply must match the complete 64-character lowercase CID. Removal must succeed
and be followed by another successful empty query. The CLI shape follows the documented
[Docker listing filters](https://docs.docker.com/reference/cli/docker/container/ls/) and
[Podman listing options](https://docs.podman.io/en/latest/markdown/podman-ps.1.html); reading those
documents does not verify an installed runtime, daemon state or container removal.

CID reads are bounded to 65 bytes and refuse links, multiple hardlinks, special files, ambiguous
short IDs and writable-by-others evidence. The canonical runtime directory and client home must
be owned mode 0700; their canonical parent must be owned and not writable by others. Directory,
CID, backend and host executable observations are rechecked around each command. Runtime control
captures use scrubbed explicit environment, a fixed external PATH, at most 4096 combined output
bytes per command, at most ten seconds per command and one narrowing-only cleanup budget capped
at thirty seconds. Warnings, nonzero exits, overflowing output, leftover owned descendants and
late finalization refuse. Original capture errors/interrupts/exits survive secondary cleanup errors.

The trusted caller must retain the exact runtime/daemon route and protect its launch CID from
untrusted writers. Boundary-local file hashes do not attest through-exec identity, dependencies,
the daemon, an image or a container hard lifetime. Emergency child reaping is mandatory even after
expiry; synchronous trusted Python/file work is not preempted. CID evidence is removed only after
successful absence and final custody checks. An error during/after that final unlink cannot roll
it back. The legacy `backend.cleanup` adapter discards the typed observation and preserves a
missing-CID no-op; a trusted started-phase finalizer must use the typed result and must reject
`NO_IDENTIFIER`, including on retries after incomplete finalization.

All cleanup process controls execute fixed finite synthetic Python fixtures with mocked runtime
replies, never Docker/Podman, target code or an image. Neither runtime is on this host's observed
PATH; actual runtime integration is INCONCLUSIVE. These observations grant no execution or
runtime authority. The trusted production launch/finalizer and real image/isolation evidence
remain required; production Hardhat stays UNAVAILABLE.

`finalize_hardhat_phase` now supplies a single-use finalization context for a trusted executor.
It joins the exact typed request, wrapper phase/CID/output paths, live bridge/binding and pinned
chain/policy observations. An exclusive retained marker outside the writable output prevents
reuse, including retries after a failed entry. The client directory is created exclusively before
environment preparation so a preexisting alias cannot redirect those writes. The marker, phase,
runtime/client/home directories and host executable identity are rechecked around the handoff.

The context yields explicit **unverified** `HardhatPhaseLaunch` inputs; the caller still must admit
their exact command/environment independently before executing them. It starts no process and
does not stop the shared bridge; the owned two-phase driver retains those responsibilities.
The context requires bounded argv, a narrowing-only absolute deadline and an unchanged frozen
launch environment. Cleanup receives that same scrubbed environment/client home plus the retained
host executable and exact CID expectations; it does not rebuild routing from changed ambient
variables. A detached backend copy retains the original cleanup selection if phase metadata changes.
These expected observations only narrow cleanup; no serialized value grants runtime authority.

After handoff a missing, invalid or changed CID, incomplete typed cleanup, changed phase selection
or late finalization cannot complete successfully. If no exact CID remains after capture, including
an auto-removal path without retained launch evidence, this context refuses rather than inferring
another identifier. A phase/request/bridge error still attempts cleanup using the original safe
runtime selection; unsafe directory/claim/executable drift prevents invoking an unrelated runtime.
Emergency cleanup uses its separate finite allowance even after result eligibility expires.
Original errors/interrupts/exits survive secondary failure, with only a fixed non-sensitive note
that finalization was incomplete. Late CID unlink cannot be rolled back, as described above.

The fixed-control integrations compose this context with the real two-phase driver, owned local
bridge and finite Node/Python children. All container launches/CID production/runtime replies are
explicit test doubles; no actual runtime, image or target JavaScript is executed. Runtime routing
is captured per phase, not independently authenticated or frozen across the complete run. A real
admission boundary must separately bind the daemon/storage route, pinned image and executable
dependencies, exact whole-command/mount policy, durable launch identity and supported containment.
File rechecks do not prove through-exec custody or a container hard lifetime; synchronous trusted
Python/file work is not preempted. Production Hardhat remains UNAVAILABLE and all execution/
runtime/readiness authority stays false. Exact validation totals are in the worklog.

### Offline image metadata prerequisite

`read_managed_image_metadata(bundle, blob_root=...)` consumes the existing managed bundle's
declared image and platform-manifest pins. The explicit local SHA-256 blob directory may be the
`blobs/sha256` directory of a supplied OCI layout. It does not discover or validate a whole layout,
follow tags/annotations/URLs, contact a registry, import an image or execute a runtime. Only two
or three digest-named metadata files are read through the existing descriptor-safe evidence I/O.
Each file is capped at 1 MiB; raw hashes and descriptor sizes are checked before JSON decoding.

The narrow supported OCI v1 subset is a direct manifest or one flat index with at most 64 entries,
one exact pinned baseline Linux amd64/arm64 selection, and at most 128 ordinary tar/gzip/zstd
layer references. Declared sizes are capped at 4 GiB per layer and 8 GiB in total; these are not
decompression allowances. Nested indexes, Docker schema-2 media types, artifacts, foreign layers,
URLs/embedded descriptor data, ambiguous platforms and unrepresented platform requirements refuse.
Config OS/architecture and layer/diff-ID count/order are joined; for uncompressed tar the declared
digest must equal its diff ID. No compressed or uncompressed layer bytes are read in this slice.

The returned frozen observation retains raw metadata, not a transferable admission. The bundle
is detached and revalidated, but its pins are still declarations. Config defaults, actual binary
architecture, filesystem overlay semantics, executable/reporter membership and runtime identity
remain unverified. A future content/execution consumer must establish those separately and
revalidate its current inputs/custody; constructing this observation grants no authority.
All trust, image-attestation, execution and readiness flags remain false. Synthetic real-file
tests exercise valid and unsafe joins without a runnable image; production Hardhat is UNAVAILABLE.

Planning correction: an OCI config ImageID is not a manifest/index digest. Content-addressed
manifest bytes can be hashed offline; a registry push is not intrinsically needed to calculate
that digest. Whether an eventual approved local import preserves a Docker/Podman `name@digest`
reference usable with the existing `--pull never` policy still needs actual runtime verification.
No import/build/pull or registry choice is selected here. See OCI v1.1.1
[config identity](https://github.com/opencontainers/image-spec/blob/v1.1.1/config.md),
[descriptors](https://github.com/opencontainers/image-spec/blob/v1.1.1/descriptor.md) and
[local image layout](https://github.com/opencontainers/image-spec/blob/v1.1.1/image-layout.md).

### Local image layer-byte identity

`verify_managed_image_layers` now consumes the current bundle and metadata automatically, then
observes exact selected uncompressed/gzip layer bytes. The new read-only `stream_file_evidence`
boundary verifies the stored file's raw digest and exact size before yielding bounded chunks.
It holds the original file/root/parent descriptors, rehashes the complete second pass, rechecks
path/inode/parent-mode custody and closes the iterator and descriptors on normal/error/interrupt
paths. Early consumer exit, changed files, late closure or an incomplete stream cannot succeed.
Trusted consumers must wait for successful context exit before accepting their computed results.

`ManagedImageLayerLimits` defaults to 4 GiB stored per layer, 8 GiB stored in total, 4 GiB expanded
per layer, 8 GiB expanded in total, 128 gzip members per layer and 120 seconds for the complete
metadata/layer/revalidation call. Explicit limits are revalidated and may be narrowed; hard maxima
are 4/8 GiB stored, 8/16 GiB expanded, 1024 members and 600 seconds. Duplicate layer references
retain their order and consume allowance for every occurrence. Each inflate call returns at most
64 KiB and can exceed the remaining expanded allowance by only the one refusal byte. Complete
concatenated gzip members are supported; CRC/size errors, truncation and unaccounted trailing or
padding bytes refuse. No unbounded decompression flush is used. Zstd metadata remains readable,
but zstd layer material refuses before layer I/O because no approved decoder is selected.

All required diff IDs must match the observed full uncompressed bytes. Current metadata and bundle
are revalidated after the layer reads, and no late result can pass. These are byte observations,
not a retained launch lease or a complete OCI/tar validator: layers are not extracted, overlay
semantics and filesystem paths are not interpreted, runtime defaults are not approved and no
executable/reporter membership or actual image/daemon identity is attested. A later content or
execution consumer must revalidate its own current bytes/custody. Synchronous OS calls and trusted
Python work cannot be forcibly preempted here; byte/deadline checks do not prove a hard process
lifetime. All image/execution/readiness authority remains false and production Hardhat UNAVAILABLE.

Safe synthetic tar/gzip controls and real-file drift/interrupt regressions are recorded in the
worklog. No layer is written by verification; no image is built, imported or executed. Sources:
[Python 3.12 zlib](https://docs.python.org/3.12/library/zlib.html),
[gzip format](https://www.rfc-editor.org/rfc/rfc1952.html) and
[OCI diff IDs](https://github.com/opencontainers/image-spec/blob/v1.1.1/config.md).

### Local image file membership

`verify_managed_image_files` revalidates the four selected bundle pins, then reuses the retained
layer stream and bounded decoder to hash regular-file payloads while interpreting a restricted
tar changeset in memory. It never extracts a path, calls a runtime, opens a network or installs an
image. Every stored/expanded hash, complete stream, custody/deadline and final metadata/bundle
check must pass before an observation is returned. Limits are revalidated at the consumer.

USTAR and bounded local PAX path/linkpath/size/numeric-owner/time/name records are supported.
Whiteouts affect lower layers only, irrespective of marker ordering. Directory-to-directory
replacement retains children; other replacement removes the old subtree. Symbolic links are
resolved component by component inside the image root, including absolute targets and dot-dot
after symbolic components. Hardlinks support earlier same-layer regular files or resolved
hardlinks with matching numeric metadata, preserving their content if the original is replaced
later. No host path resolution is used. Forward/lower-layer hardlinks, writes through symbolic
parents, ambiguous same-layer ancestors, unknown/global/GNU/sparse/device/FIFO/xattr semantics,
duplicate normalized names, malformed records and nonzero padding refuse explicitly.

`ManagedImageFileLimits` composes the existing layer allowances with default per-layer limits of
100,000 headers, 16 MiB metadata, 64 KiB per local PAX header, 4096 UTF-8 path bytes and 128 path
components. Image-wide defaults are 250,000 headers, 32 MiB cumulative metadata, 100,000 live nodes,
32 MiB live path text and 40 symbolic-link hops. Hard maxima are respectively 500,000 headers,
64 MiB metadata, 1 MiB PAX, 16,384 path bytes, 256 components, 500,000 total headers, 64 MiB total
metadata, 250,000 nodes, 64 MiB live path text and 40 hops. Per-layer limits bound parsing before
the cumulative total is checked. No regular-file payload is retained in the in-memory view.
The shared layer deadline also covers parsing, overlay application and final file lookup; it is
not hard process preemption or a retained launch lease.

Hardhat, Node and the loopback relay use their existing fixed image executable locators. The
reporter package pin now joins the fixed image path `/usr/local/lib/mmaudit/hardhat_reporter.cjs`
and must equal the compiled reporter source hash/version. Its future image construction and
actual launch-path consumption are not implemented by this static check. Selected file mode bits
must be nonspecial and not group/world writable; executable roles require an execute bit and the
reporter a read bit. These are archive declarations, not proof of runtime ownership, permission,
tool version, executable architecture, interpreter/library/package closure or executed identity.
All returned trust/execution/readiness authorities remain false. Production Hardhat is still
UNAVAILABLE; no real image/container or unattended audit is demonstrated.

Sources: [OCI v1.1.1 layer semantics](https://github.com/opencontainers/image-spec/blob/v1.1.1/layer.md)
and [Python 3.12 fixed-header/PAX reference implementation](https://github.com/python/cpython/blob/3.12/Lib/tarfile.py).
The POSIX documentation site was unavailable during inspection; unsupported formats were not
inferred to pass. Synthetic positive/negative controls and exact local validation are in the worklog.

## External evaluation

A private holdout and independently adjudicated professional comparison are not
present. That optional human-comparison tier remains `NOT_DEMONSTRATED` and is
objective-out-of-scope for frozen synthetic/public completion. The current objective's
superiority verdict must instead come from precommitted public ground truth,
cross-lineage automated adjudication, and the objective-pinned benchmark authority;
absence of a commissioned human auditor is not a completion blocker.
